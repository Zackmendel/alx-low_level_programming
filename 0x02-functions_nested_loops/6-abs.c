#include "main.h"

/**
 * int _abs(int) - short description
 * function computes the absolute value of an integer
 * Return: Absolute value of number
 */
int _abs(int a)
{
	if (a >= 0)
		return (a);
	else
		return (n * -1);
}

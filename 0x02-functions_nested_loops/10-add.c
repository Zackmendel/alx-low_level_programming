#include "main.h"
/**
 * add - short description
 * @a: an integer
 * @b: an integer
 * function that adds two integers, a & b and returns the result
 *
 * Return: result
 */
int add(int a, int b)
{
	return (a + b);
}

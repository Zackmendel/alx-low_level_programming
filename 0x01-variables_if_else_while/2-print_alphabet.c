#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main() 
{
	char ch;
	/* sets loop for alphabets*/
	for (ch = 'a'; ch <= 'z'; ch++)
	{
	putchar(ch);
	}
	return 0;
}

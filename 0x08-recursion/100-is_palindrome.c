#include "main.h"
#include <stdio.h>

int _strlen_recursion(char *s);
int helper_palindrome(char *s, int len);

/**
 * is_palindrome - funtion that returns 1 if a string is
 * a palindrome and o if not
 *
 * @s: string input
 *
 * Return: 1 if a string is a palindrome and 0 if not
 */

